{
  "name": "@hivemind/setup",
  "version": "0.1.0",
  "description": "Setup wizard and configuration tools",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "hivemind-setup": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "inquirer": "^9.0.0",
    "chalk": "^5.0.0"
  },
  "devDependencies": {
    "@types/inquirer": "^9.0.0",
    "typescript": "^5.3.0",
    "vitest": "^1.0.0"
  }
}

